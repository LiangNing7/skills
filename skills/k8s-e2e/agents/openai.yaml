# Codex-specific metadata for this skill. Claude Code ignores this file.
name: "k8s-e2e"

interface:
  display_name: "Kubernetes End-to-End Tests"
  short_description: "Exercise the reconcile link against a running cluster"

policy:
  allow_implicit_invocation: true
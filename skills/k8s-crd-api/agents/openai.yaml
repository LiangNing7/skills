# Codex-specific metadata for this skill. Claude Code ignores this file.
name: "k8s-crd-api"

interface:
  display_name: "Kubernetes API Packages"
  short_description: "Create internal and versioned APIs under pkg/apis"

policy:
  allow_implicit_invocation: true

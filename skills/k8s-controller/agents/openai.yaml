# Codex-specific metadata for this skill. Claude Code ignores this file.
name: "k8s-controller"

interface:
  display_name: "Kubernetes Controller"
  short_description: "Reconcile served resources with controller-runtime"

policy:
  allow_implicit_invocation: true
# Codex-specific metadata for this skill. Claude Code ignores this file.
name: "k8s-controller-config"

interface:
  display_name: "Kubernetes Controller Config"
  short_description: "Author controller-manager component config (shared + per-domain)"

policy:
  allow_implicit_invocation: true
